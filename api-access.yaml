kind: role
metadata:
  name: api-access
spec:
  allow:
    rules:
      - resources:
          - session_tracker
        verbs:
          - list
          - read
  deny: {}
  options:
    max_session_ttl: 87600h0m0s
version: v7
